services:
  - type: web
    name: lyvica-scoring
    runtime: python
    buildCommand: pip install -e .
    startCommand: uvicorn web.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
